version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
   version: 3.9
   install:
      - requirements: docs/requirements.txt
      - method: pip
        path: .
        extra_requirements:
            - docs
